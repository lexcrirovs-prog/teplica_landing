# Проверка Premium E — 2026.09.10.4

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6).

Статус: **PASSED_LOCAL / release-candidate**. Проверка новой схемы относительно версии .3, коммит `877a9117f973d363d205cc19039f75e5f3c2963c`.

## Объём изменений

- Добавлены `condenser.webp` и `greenhouse.webp`, созданные встроенным ImageGen по предоставленным фото и чертежу.
- Конденсор: 1000×699, RGB, 27 338 байт; светлый фон близок к #f7f9fa. Теплица: 1000×667, RGBA, 98 814 байт. Все четыре вида схемы: 175 232 байта.
- Котёл и горелка остаются видами из CAD предыдущего выпуска. Новые изображения являются схематичными иллюстрациями, без заявленных размеров и инженерного подтверждения компоновки.
- Перенаправлены линии к видимым входам/выходам конденсора; уменьшены наконечники стрелок. Декоративные соединения не перехватывают нажатия.
- Под схемой раскрываются четыре крупных вида. Галерея: два столбца на широком экране, один на мобильном.
- Калькулятор, формы, цены и серверная обработка не менялись.

## Автоматические проверки

- `node --test tests/*.test.cjs`: **22/22 PASS**. Проверяются расчёты и прайс, контент, изображения, контракт формы и конфигурация сервера.
- `node scripts/validate-page.cjs`: **PASS**, 63 уникальных ID, 38 локальных ссылок, версия 2026.09.10.4.
- `node --check script.js`: **PASS**.
- `git diff --check`: **PASS**.
- Новые WebP открываются, имеют реальные заявленные размеры. SHA-256 записаны в `scheme-illustrations-2026-09-10.json`.
- Независимое ревью изменений от .3 не выявило блокеров или регрессий.

## Браузерная проверка

Локальный адрес: http://127.0.0.1:4178/#scheme. После изменений страница перезагружалась.

**1440×1000:**

- Осмотрена вся схема: форма и детали конденсора, три пролёта теплицы, маршруты и направления стрелок, читаемость подписей.
- Все шесть узлов выбраны клавишей Enter. Заголовок карточки и `aria-pressed` соответствуют выбранному узлу; выбранная текстовая кнопка одна.
- Конденсор выбран мышью непосредственно на изображении. Карточка и выделение обновились.
- Раскрыт блок крупных видов. Все четыре изображения загрузились с `naturalWidth=1000`, размеры по высоте 700 / 700 / 699 / 667.
- Осмотрены оба ряда галереи. Нет шахматного фона, обрезанных деталей или перекрытия подписей.
- Ширина документа 1425 при viewport 1440: горизонтального переполнения страницы нет.

**390×844:**

- Выбрана текстовая кнопка «Теплица». Показаны соответствующие описание и параметры.
- Горизонтальная прокрутка доведена до теплицы; изображение, активная рамка и подвод газа видны.
- Ширина документа 375, доступная ширина схемы 334, её прокручиваемое содержимое 800. Прокрутка ограничена схемой.
- Галерея перешла в один столбец (299,2 px). Крупные конденсор и теплица осмотрены на мобильной ширине, подписи читаемы.
- Блок крупных видов закрывается штатным элементом details/summary.

Консоль браузера: список error/warn пуст. Временные размеры viewport сброшены. Предпросмотр оставлен на версии .4; проверено значение application-version.

## Границы проверки

Это проверка в браузере с изменением viewport. Физический телефон, системная печать и доставка писем в этой версии **NOT_RUN**. GitHub Actions: **CI_NOT_CONFIGURED**. Публикация не выполнялась, `production_deployed=false`.

Исходные фотографии, чертёж, CAD и презентация сохранены отдельно. В публичный пакет входят только WebP; оригиналы генераций и история неудачных вариантов остаются локально. Контрольные суммы референсов и итоговых файлов приведены в документации происхождения.
