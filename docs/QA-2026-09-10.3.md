# Проверка 2026.09.10.3: модели оборудования на схеме

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6). Статус **PASSED_LOCAL**. Публикация на рабочем сервере не выполнялась.

## Источники и изображения

- Пользовательский STEP котла Premium E-7000 преобразован в сетку через OpenCascade, затем отрендерен в Blender. Передний торец с горелочным фланцем и петлями проверен визуально; корпус уходит вправо. Источник содержит 732 тела; для внешней иллюстрации сохранены 668 наружных тел, скрыты 64 внутренние оребрённые трубы. Оригинальный STEP и полная тесселяция сохранены отдельно.
- Геометрия горелки извлечена из RS_610.dwg через отдельную AutoCAD Core Console: 168 сеток, 2328 треугольников. Проверены мм, ось сопла и габариты. RFA для геометрии не использовался. Цвета исходного DWG сохранены.
- Оба PNG просмотрены, затем перекодированы в WebP без изменения композиции: 1000×700 RGBA. Котёл — 28 420 байт, горелка — 20 660 байт, суммарно 49 080 байт.
- Исходные STEP, DWG, RFA и PPTX повторно сверены по SHA-256: изменений нет. Контрольные суммы, размеры и ограничения записаны в model-sources-2026-09-10.json. CAD, GLB, BLEND и презентация в публичный пакет не входят.
- Все 13 слайдов презентации прочитаны и отрендерены PowerPoint ReadOnly/без окна. Использованы слайды 5 и 8: холодная вода конденсора, защищённая обратка котла и накопление тепла в баке. Непроверенные экономические числа и декоративные фоны презентации не переносились.

## Проверки сайта

| Проверка | Результат |
|---|---|
| Node-проверки | 22/22 PASS, включая бюджет двух изображений схемы |
| Структура страницы | 62 уникальных ID, 34 локальные ссылки, метаданные .3 согласованы |
| JS и git diff --check | PASS |
| Desktop 1440×1000 | Модели, температуры и линии читаемы; горизонтального переполнения страницы нет |
| Горелка — мышь | Изображение выбирается, текст и состояние кнопок обновляются |
| Котёл — Enter | Выбирается с клавиатуры, aria-pressed обновляется |
| Крупные виды | Раскрываются; оба изображения полностью загружены с naturalWidth=1000, naturalHeight=700 |
| Переход к сезонным режимам | Ссылка #season раскрывает родительский details и показывает раздел |
| Mobile 390×844 | Страница без горизонтального переполнения; схема прокручивается внутри своего блока, текстовые кнопки доступны; крупные виды помещаются в одну колонку |
| Мобильный выбор конденсора | Выбранный узел и пояснение о температурных контурах обновляются |
| Независимое ревью | Новых блокеров кода/содержания не найдено |

Проверка мобильного вида — изменение ширины браузера, не физический телефон. Исходные формулы, цены, логика заявки и печати в этой версии не менялись. Проверки прежней версии .2 сохранены в QA-2026-09-10.md.

## Границы результата

На сайте находятся статические виды, полученные из 3D-моделей, а не интерактивный CAD-просмотрщик. Масштаб и компоновка схемы условные. Каталог RS 610 указывает 1000–6250 кВт; его совместимость с котлом E-7000 не подтверждена. В интерфейсе это иллюстрация узла, а в пояснении сказано о подборе горелки под мощность и режим. Числовые характеристики Low-NOx/FGR не приписываются показанной модели.

Сцены Blender и производные сетки находятся в локальном output/teplica-models-20260910. Материалы котла выбраны для иллюстрации и не являются спецификацией окраски. Публикация, серверный PHP/Apache, отправка писем и физическое устройство — **NOT_RUN**. CI — **CI_NOT_CONFIGURED**.
