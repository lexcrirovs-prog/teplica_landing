# Проверка Premium E — 2026.09.10.5

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6).

Статус: **PASSED_LOCAL / release-candidate**. Изменения проверены относительно `1408a79c59060d02cbb3bfa14920a5ca24e5f189`, версия .4.

## Проверенный результат

- Добавлена схематичная дымовая труба по фотографиям пользователя: один ствол, опорная ферма, секционные стыки, крепления и боковой фланец.
- Изображение создано встроенным ImageGen. После одного исправления фона выбран RGB на светлом фоне, близком к #f7f9fa. Шахматный вариант исключён. Это иллюстрация по референсам, без точных CAD-размеров.
- `chimney.webp`: 600×1178, RGB, 20 376 байт. Пять изображений схемы: 195 608 байт.
- SVG увеличен по высоте до 540; общий ряд смещён на 80 единиц. Труба занимает отдельное место сверху, резервная линия подходит к её боковому входу. SVG ограничивает пустые боковые поля, полная картинка сохраняется.
- Согласованы подпись, доступное название, кнопка и карточка «Дымовая труба». Ключ `data-node="stack"` и обработчики сохранены.
- Вместо численных порогов газоанализа карточка трубы показывает её назначение, переключение шибером и резервный режим. Подробный раздел газоанализа сохранён.
- Добавлен пятый вид в галерею. Высота изображения 520 px на широком экране, 390 px на мобильном. Калькулятор, формы, цены и обработчики взаимодействия не изменялись.

## Автоматические проверки

- `node --test tests/*.test.cjs`: **22/22 PASS**.
- `node scripts/validate-page.cjs`: **PASS**, 64 уникальных ID, 40 локальных ссылок, версия .5.
- `node --check script.js`: **PASS**.
- `git diff --check`: **PASS**.
- WebP открывается, размеры и SHA-256 совпадают с документом происхождения.
- Независимое ревью не выявило новых блокеров или регрессий.

## Браузер

Локальный адрес: http://127.0.0.1:4178/#scheme. После изменений выполнялась перезагрузка.

При **1440×1000** осмотрены общий вид схемы, подписи, целая труба и точка подключения резервной ветки. Нажатие на область трубы (`model-hit`) открывает карточку «Дымовая труба». Enter на узле также открывает эту карточку и устанавливает `aria-pressed=true` у соответствующей кнопки. Видимый фокус сохранён.

Раскрыта галерея. Все пять изображений загружены (`complete=true`, ненулевая естественная ширина). Новый файл имеет естественные размеры 600×1178. Крупная труба осмотрена целиком: высота 520 px, подписи справа, без обрезанных деталей и нарисованной шахматки. Ширина документа 1425 при viewport 1440.

При **390×844** проверены выбор текстовой кнопкой, описание узла, нажатие на изображение трубы в схеме, целое изображение и крупный просмотр. Ширина документа 375, видимая ширина схемы 334, прокручиваемое содержимое 800. Горизонтальное переполнение ограничено схемой. Галерея в один столбец шириной 299,2 px; труба высотой 390 px и подписи помещаются в блок.

Галерея закрывается через details/summary. Консоль error/warn пуста. Временный viewport сброшен, в предпросмотре проверена версия 2026.09.10.5.

## Границы

Проверка выполнена в браузере с изменением viewport. Физический телефон, системная печать и реальная доставка письма в .5 **NOT_RUN**. GitHub Actions **CI_NOT_CONFIGURED**. Публикация не выполнялась, `production_deployed=false`.

Исходные фотографии, предыдущие CAD-виды и оригинал новой генерации сохранены отдельно. В публичный пакет входит WebP; референсы и исходный PNG туда не включаются. Контрольные суммы приведены в `chimney-illustration-2026-09-10.json`.
