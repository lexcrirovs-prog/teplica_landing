# Проверка Premium E — 2026.09.10.9

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6). База: `7491191d4e3fca63c175e1f826124a35969740b9` (.8).

Статус: **PASSED_LOCAL / release-candidate**.

## Изменения

- Семь превью содержат по две отдельные ссылки-кнопки: YouTube и ВК Видео. Превью больше не является одной общей ссылкой; вложенных ссылок нет.
- Под карточками оставлены только названия этапов. Общая пара ссылок на каналы с логотипами размещена рядом с заголовком блока. Всего 14 ссылок-кнопок и две общие ссылки.
- Сохранены прежние назначения: шесть прямых роликов YouTube; YouTube второй сборки и все ВК ведут на каналы. Канальные назначения явно отражены в title и aria-label. Прямые ролики ВК не заявлены.
- Два локальных SVG: YouTube 828 байт, ВК Видео 2 369 байт. Происхождение и SHA-256: [video-brand-assets-2026-09-10.json](research/video-brand-assets-2026-09-10.json). В SVG ВК сохранена MIT-лицензия; пользовательский размер кнопки не заявляется как полное соответствие рекомендациям брендбука YouTube.
- Локальный сервер просмотра теперь выдаёт SVG из assets с image/svg+xml; PHP по-прежнему не выдаётся.

## Проверки

- `node --test tests/*.test.cjs`: **22/22 PASS**.
- `node scripts/validate-page.cjs`: **PASS**, 65 уникальных ID, 60 локальных ссылок, версия .9.
- `node --check script.js`, `node --check scripts/preview.cjs`, `git diff --check`: **PASS**.
- HTML-парсер подтвердил отсутствие вложенных ссылок. Все 14 назначений сверены с ранее проверенным соответствием этапов; старых production-platforms и общего play нет. По одной общей подписи каждого канала.
- Все 37 прежних assets и script.js, handler.php, styles.css совпадают с .8 (текст сравнивался с нормализацией CRLF/LF).
- Оба SVG разобраны XML-парсером: нет script, foreignObject, обработчиков событий и внешних href. Сверены SHA-256 и размеры. HTTP для обоих SVG: 200, image/svg+xml, тело совпадает с файлом. handler.php: 404 в preview, исходный код не выдаётся.

## Браузер

Локальный URL: http://127.0.0.1:4178/#factory.

При **1440×1000** осмотрены все семь карточек и общая строка каналов. Каждая кнопка имеет размер 48×48 px. Все логотипы загрузились; пропорции YouTube и красный градиент ВК сохранены. Ширина документа 1425 px.

При **390×844** осмотрены верхний и нижний ряды. Кнопки видны без наведения, размер каждой 44×44 px. Общие ссылки каналов расположены под заголовком. Ширина документа 375 px, горизонтального переполнения нет. Проверка elementFromPoint в центрах двух кнопок первой карточки вернула соответствующие независимые ссылки. Tab переводит фокус с YouTube на ВК, видимая обводка solid.

Первичная проверка выявила, что preview не разрешал SVG; расширение и MIME добавлены, сервер перезапущен и загрузка повторно проверена. В завершённой проверке консоль error/warn пуста. Временный viewport сброшен; открыт блок производства.

## Границы

Внешние ролики/каналы в этой правке не открывались и не воспроизводились; сохранение ссылок проверено по точным URL и доступным областям нажатия. Их предыдущая проверка описана в [production-videos-2026-09-10.json](research/production-videos-2026-09-10.json).

Физические телефоны, PHP/Apache будущего адреса, системная печать и отправка/получение писем **NOT_RUN**. GitHub Actions **CI_NOT_CONFIGURED**. Публикация не выполнялась, production_deployed=false.
