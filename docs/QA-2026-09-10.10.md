# Проверка Premium E — 2026.09.10.10

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6). База: 323d999c5f70bfef8951a32aa74943b6d5ebdd9b (.9).

## Локальная проверка

- В общей ссылке на канал ВК использован приложенный facebook_298917.png (фактически JPEG 1200×630); на семи обложках — images.png (PNG 447×447). Исходные RGB/RGBA-пиксели совпадают с lossless WebP, исходные файлы не изменены.
- Полный логотип размещён в окне 116×28 px. CSS скрывает пустые поля и смешивает белый фон с фоном раздела; сами изображения не обрезались и не перекрашивались.
- На 1440×1000 и 390×844 проверены полный логотип, семь отдельных знаков, отсутствие переполнения. Все восемь новых вхождений загрузились. Имеющиеся кнопки YouTube/ВК и адреса назначения сохранены.
- `node --test tests/*.test.cjs`: 22/22 PASS. `node scripts/validate-page.cjs`: 65 уникальных ID, 60 локальных ссылок, версия .10 PASS. Синтаксис script.js и diff: PASS.
- Публикация на /teplica8/ прямо разрешена текущим сообщением пользователя. На read-only preflight каталог отсутствовал, HTTP404; доступ SSH и корень сайта подтверждены.

## Публикация и приёмка

**PUBLISHED_VERIFIED** — https://prgz.ru/teplica8/.

- В staging и после переименования сверены точный набор 48 файлов, размеры и SHA-256. Файлы создавались внутри public_html для наследования ACL. Прежней teplica8 не было; посторонние файлы не перезаписывались.
- Все 46 статических файлов доступны по HTTPS200 и совпадают с manifest. .htaccess и PHP-исходник сверены по SSH. PHP5.6.40 syntax PASS, GET handler.php возвращает405 с error=method_not_allowed.
- На живой странице в1440×1000 и390×844 проверены восемь вхождений предоставленных логотипов, канальные ссылки, отсутствие горизонтального переполнения. Мобильные кнопки44×44px. Консоль error/warn пуста.
- Подбор: ввод2,4га даёт E-2500; значение восстановлено на3га. Мобильное меню переводит к технологии и закрывается. Просмотр пяти изображений оборудования раскрывается и закрывается.
- После подтверждения публикации version.json атомарно обновлён на published / production_deployed=true; итоговый комплект повторно сверен с сервером и по HTTPS.
- Контрольные суммы соседних сайтов и корневых файлов не изменились. Отчёт с контрольными суммами: [deployment-2026-09-10.10.json](deployment-2026-09-10.10.json).

Отправка/доставка писем, воспроизведение внешних видео, физические телефоны и системная печать NOT_RUN. GitHub Actions CI_NOT_CONFIGURED.
