# Проверка версии 2026.09.10.1

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6).

## Результат

**PASSED_LOCAL.** Версия подготовлена для оценки дизайна. Публикация на рабочем сервере не выполнялась.

| Проверка | Результат |
|---|---|
| `node --test tests/*.test.cjs` | 21/21: модельный ряд, цены, каскад, экономика, CO₂, контент, изображения, PHP-контракт и конфигурация |
| `node scripts/validate-page.cjs` | 62 уникальных ID; 30 локальных ссылок на ресурсы; метаданные версии согласованы |
| `node --check script.js` и `node --check scripts/preview.cjs` | Синтаксис корректен |
| Дополнительный разбор HTML | Вложенность тегов и details сбалансирована; у всех 22 изображений заданы alt, width, height |
| Desktop 1440×900; также 1280×720 | Нет горизонтального переполнения; просмотрены первый экран, производство, схема, расчёт |
| Tablet 768×1024 | Нет горизонтального переполнения и выходящих за ширину видимых элементов вне намеренно прокручиваемой схемы |
| Mobile 390×844 и 360×800 | Нет горизонтального переполнения; просмотрены первый экран, комплектация и расчёт |
| Мобильное меню | Открывается, закрывается после выбора, переводит фокус на целевой заголовок |
| Площадь 0,3 / 2,4 / 11 / 50 га | E-1000 / E-2500 / 2 × E-6000 / 5 × E-10000 |
| Ручная мощность 3875 кВт | Округление до 3900 кВт и подбор E-4000; газовый и электрический сценарии переключаются |
| Площадь 100 га и пустое поле | Ошибка ввода; результаты и печать скрыты до исправления |
| Максимальная комплектация | 12 узлов; описание и отметки обновляются |
| Схема | Выбор конденсора обновляет пояснение и параметры; восстановлен видимый газовый тракт SVG |
| Зимний режим | CO₂ отмечен как остановленный; накопленное тепло направлено к отоплению теплицы |
| Вентиляция 100% | CO₂-подкормка останавливается с пояснением |
| Комментарий и данные объекта | Ручной текст сохраняется при выборе комплектации и изменении площади |
| Печатный CSS через браузерную эмуляцию media=print | Основная страница скрыта, расчёт и актуальные значения показаны |
| Журнал браузера | В просмотренном журнале ошибок JavaScript не выявлено |

Изменение размеров окна — эмуляция в браузере. Проверка на физических телефонах и планшетах **NOT_RUN**. Сохранение физического PDF-файла и печать на бумаге **NOT_RUN**. Проверено печатное представление.

## Исправлено по независимому ревью

- Обрезанный фокус числовых полей заменён заметной рамкой группы.
- Программные переходы переводят клавиатурный фокус к целевому заголовку.
- Ввод вне диапазона больше не подменяется допустимой величиной без сообщения.
- Подписи зимней схемы согласованы с остановкой CO₂ и отдачей тепла.
- Исправлены размеры изображений и нулевой bounding box градиента горизонтальной SVG-линии.

## Границы проверки

Рабочий `handler.php` сохранён. Локальный сервер отвечает на POST кодом 503 и не исполняет PHP. Реальные заявки, письма и подтверждение доставки **NOT_RUN**. Прайс от 13.07.2026 и исходные расчётные допущения не актуализировались коммерчески. Инженерные параметры перенесены из исходного сайта; независимая инженерная сертификация не входит в редизайн. GitHub Actions **CI_NOT_CONFIGURED**.
