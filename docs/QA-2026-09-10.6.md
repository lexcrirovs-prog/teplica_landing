# Проверка Premium E — 2026.09.10.6

Дата: 10.09.2026. Исполнитель: OpenAI Codex (GPT-6).

Статус: **PASSED_LOCAL / release-candidate**. База: `0997aa2467d3388abae2f91e3e2564bda4c7a493` (.5).

## Изменения

В копии сцены горелки скрыты 102 отдельные детали логотипа EXECO и его стрелок. Все 66 деталей оборудования, геометрия, материалы, камера и освещение сохранены. Альфа исходного и нового PNG побайтно одинакова. Повторный рендер даёт небольшие отличия отдельных пикселей на кромках; пиксельная идентичность всего RGB не заявляется.

В копии сцены котла двум телам верхней панели 0471/0472 назначен светлый материал. Патрубки и крепёж сохранены с прежними материалами для читаемости. Геометрия, камера, свет и разрешение совпадают; число сэмплов увеличено с 64 до 256. Материал панели изменён намеренно. Исправление не сводится к повторному кодированию прежнего WebP.

Выбраны native PNG из Blender. Генеративные варианты с непрозрачной шахматной подложкой отклонены. Финальные WebP 1000×700 RGBA закодированы quality=88, method=6, exact=true; alpha побайтно совпадает с соответствующими PNG. Размеры: котёл 30 528 байт, горелка 19 734 байта; все пять изображений схемы — 196 790 байт. Остальные три WebP совпали с базовым коммитом.

Исходные CAD и Blender-сцены сохранены. Новые сцены и рендеры находятся отдельно от репозитория в `output/teplica-scheme-20260910-v6/`. Метаданные и хеши: [model-cleanup-2026-09-10.json](model-cleanup-2026-09-10.json).

## Проверки

- `node --test tests/*.test.cjs`: **22/22 PASS**.
- `node scripts/validate-page.cjs`: **PASS**, 64 уникальных ID, 40 локальных ссылок, версия .6.
- `node --check script.js`: **PASS**.
- `git diff --check`: **PASS**.
- В diff страницы изменены только версия и четыре ссылки на изображения; SVG-геометрия, подписи, расчёты и формы не менялись.

## Браузер

Локальный адрес: http://127.0.0.1:4178/#scheme.

При **1440×1000** осмотрены схема и оба крупных вида: логотип отсутствует, верхняя панель светлая, фланцы и крепёж читаются, прозрачный фон корректен. Все пять изображений загружены с ожидаемыми размерами. Версия страницы .6 и cache-query обоих изображений подтверждены по DOM. Ширина документа 1425 px. Нажатие на изображение горелки открывает её карточку и устанавливает `aria-pressed=true`.

При **390×844** осмотрен мобильный столбец с котлом и горелкой: изображения по 299,2×209,44 px, без обрезки. Ширина документа 375 px. Enter на узле котла открывает его карточку и устанавливает `aria-pressed=true`. Галерея закрывается; временный viewport сброшен. Консоль error/warn пуста.

## Границы

Проверка выполнена в браузере с изменением viewport. Физический телефон, системная печать и реальная доставка письма в .6 **NOT_RUN**. GitHub Actions **CI_NOT_CONFIGURED**. Внешняя приёмка и публикация не выполнялись, `production_deployed=false`.

Независимый предварительный просмотр diff подтвердил ограниченный состав изменений. Полное заключение второго агента не получено: выполнение остановилось по лимиту. Финальные файлы и метаданные проверены основным агентом.
